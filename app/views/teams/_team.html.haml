%div.listItem
  %span.listItemTitle= team.name
  %br
  %span.listItemDetail Scrum Master: #{ link_to(team.scrum_master.name, user_path(team.scrum_master.id)) if team.scrum_master }
  %br
  %span.listItemDetail Product Owner: #{ link_to(team.product_owner.name, user_path(team.product_owner.id)) if team.product_owner }
  %br
  %span.listItemDetail Members:
  - team.members.collect do |p| 
    = link_to(p.name, user_path(p.id))
    
  %br
  - if can? :manage, team
    %a{ :href => edit_team_path(team.id) } Edit
